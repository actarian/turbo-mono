import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Split } from '@websolute/ui';

<Meta
  title="Sections / Split"
  component={Split}
/>

# Split

A component to display a `Split`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/sections/split)

## Imports

```js
import { Split } from '@websolute/ui';
```

## Example

A basic example of `Split`.

<Canvas>
  <Story name="Split">
    <Split />
  </Story>
</Canvas>
