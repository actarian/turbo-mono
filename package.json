{
  "name": "mono",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --filter=!theme",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "start": "turbo run start --scope=web",
    "icons": "turbo run dev --filter=icons"
  },
  "devDependencies": {
    "eslint-config-websolute": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "dependencies": {},
  "engines": {
    "npm": ">=8.5.5",
    "node": ">=16.15.0"
  },
  "packageManager": "npm@8.5.5"
}
