import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Colors } from '@ui-sections';

<Meta
  title="Colors"
  decorators={[(story) => (
    <div style={ { padding: '0' } }>
      { story() }
    </div>
  )]}
  parameters={{
    backgrounds: { disable: true, grid: { disable: true } },
  }}
/>

# Colors

<Canvas>
  <Story name="Colors">
    <Colors />
  </Story>
</Canvas>
