import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Select, SelectProps, Field, Label } from '@websolute/ui';
import Link from 'next/link';

<Meta
  title="Forms / Select"
  component={Select}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%', display: 'flex', justifyContent: 'center' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Select

A component to display a `Select`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/forms/select)

## Imports

```js
import { Select } from '@design-system';
```

## Example

A basic example of `Select`.

<Canvas>
  <Story name="default">
    <div style={{ padding: '4rem', fontSize: '2rem', width: '100%', maxWidth: '500px' }}>
      <Field>
        <Label>Select</Label>
        <Select name='country' id='country' defaultValue='Italia'>
          <option>Italia</option>
          <option>Canada</option>
          <option>Francia</option>
          <option>Germania</option>
          <option>Stati Uniti</option>
        </Select>
      </Field>
    </div>
  </Story>
</Canvas>
