import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Layout, LayoutProps } from '@websolute/ui';

<Meta
  title="Components / Layout"
  component={Layout}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Layout

A component to display a `Layout`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/layout)

## Imports

```js
import { Layout } from '@websolute/ui';
```

## Example

A basic example of `Layout`.

<Canvas>
  <Story name="Layout">
    <Layout>Layout</Layout>
  </Story>
</Canvas>
