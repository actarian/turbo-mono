import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Header, HeaderProps } from '@websolute/ui';

<Meta
  title="Sections / Header"
  component={Header}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Header

A component to display a `Header`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/sections/header)

## Imports

```js
import { Header } from '@websolute/ui';
```

## Example

A basic example of `Header`.

<Canvas>
  <Story name="default">
    <div style={{ backgroundColor: 'var(--color-neutral-900)' }}>
      <Header />
    </div>
  </Story>
  <Story name="sticky">
    <Header sticky />
  </Story>
</Canvas>
