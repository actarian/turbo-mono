import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Input, InputProps } from './input';
import { Field, Label } from '@ui-forms';
import Link from 'next/link';

<Meta
  title="Forms / Input"
  component={Input}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%', display: 'flex', justifyContent: 'center' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Input

A component to display a `Input`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/forms/input)

## Imports

```js
import { Input } from '@design-system';
```

<!--
## Props

<ArgsTable of={InputProps} />
-->

## Example

A basic example of `Input`.

<Canvas>
  <Story name="default">
    <div style={{ padding: '4rem', fontSize: '2rem', width: '100%', maxWidth: '500px' }}>
      <Field>
        <Label>First name</Label>
        <Input placeholder="First name" />
      </Field>
    </div>
  </Story>
</Canvas>
