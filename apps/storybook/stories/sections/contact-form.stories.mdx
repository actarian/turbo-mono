import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { ContactForm, ContactFormProps } from '@websolute/ui';

<Meta
  title="Sections / ContactForm"
  component={ContactForm}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# ContactForm

A component to display a `ContactForm`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/sections/contact-form)

## Imports

```js
import { ContactForm } from '@websolute/ui';
```

## Example

A basic example of `ContactForm`.

<Canvas>
  <Story name="ContactForm">
    <ContactForm />
  </Story>
</Canvas>
