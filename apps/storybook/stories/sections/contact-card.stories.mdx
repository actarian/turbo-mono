import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { ContactCard, ContactCardProps, Grid } from '@websolute/ui';
import { ContactCardDefaults } from '@websolute/mock';

<Meta
  title="Sections / ContactCard"
  component={ContactCard}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# ContactCard

A component to display a `ContactCard`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/sections/contact-card)

## Imports

```js
import { ContactCard } from '@websolute/ui';
```

## Example

A basic example of `ContactCard`.

<Canvas>
  <Story name="ContactCard">
    <Grid.Row columnGap="1rem" rowGap="1rem" margin="2rem">
      <Grid sm={6} md={4} lg={3}>
        <ContactCard item={ContactCardDefaults.item} height="100%" />
      </Grid>
    </Grid.Row>
  </Story>
</Canvas>
