import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Divider, DividerProps, Media, Text } from '@websolute/ui';

<Meta
  title="Components / Divider"
  component={Divider}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Divider

A component to display a `Divider`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/divider)

## Imports

```js
import { Divider } from '@websolute/ui';
```

## Example

A basic example of `Divider`.

<Canvas>
  <Story name="Divider">
    <Divider></Divider>
  </Story>
</Canvas>

<Canvas>
  <Story name="Divider with title">
    <Divider>Title</Divider>
  </Story>
</Canvas>
