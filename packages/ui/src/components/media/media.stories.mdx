import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Media, MediaProps } from './media';

<Meta
  title="Components / Media"
  component={Media}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Media

A component to display a `Media`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/media)

## Imports

```js
import { Media } from '@design-system';
```

<!--
## Props

<ArgsTable of={MediaProps} />
-->

## Example

A basic example of `Media`.

<Canvas>
  <Story name="Media">
    <Media width="320px">
      <img src={'https://picsum.photos/640/480'} />
    </Media>
  </Story>
</Canvas>
