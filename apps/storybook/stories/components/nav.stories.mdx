import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Nav, NavProps, Text, Button } from '@websolute/ui';
import Link from 'next/link';

<Meta
  title="Components / Nav"
  component={Nav}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Nav

A component to display a `Nav`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/nav)

## Imports

```js
import { Nav } from '@websolute/ui';
```

## Example

A basic example of `Nav`.

<Canvas>
  <Story name="Nav.Row">
    <Nav.Row gap="3rem">
      <Link href="#marketing"><Button variant="nav" as="a">Solutions</Button></Link>
      <Link href="#analytics"><Button variant="nav" as="a">Company</Button></Link>
      <Link href="#commerce"><Button variant="nav" as="a">Magazine</Button></Link>
      <Link href="#insights"><Button variant="nav" as="a">More</Button></Link>
    </Nav.Row>
  </Story>
</Canvas>

<Canvas>
  <Story name="Nav.Col">
    <Nav.Col marginBottom="2rem" style={{ fontSize: '0.9rem' }}>
      <Text size="8">Company</Text>
      <Link href="#about"><Button variant="nav" as="a">About</Button></Link>
      <Link href="#blog"><Button variant="nav" as="a">Blog</Button></Link>
      <Link href="#jobs"><Button variant="nav" as="a">Jobs</Button></Link>
      <Link href="#press"><Button variant="nav" as="a">Press</Button></Link>
      <Link href="#partners"><Button variant="nav" as="a">Partners</Button></Link>
    </Nav.Col>
  </Story>
</Canvas>
