import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Split } from '@websolute/ui';
import { SplitDefaults } from '@websolute/mock';

<Meta
  title="Sections / Split"
  component={Split}
/>

# Split

A component to display a `Split`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/sections/split)

## Imports

```js
import { Split } from '@websolute/ui';
```

## Example

A basic example of `Split`.

<Canvas>
  <Story name="Split">
    <Split item={SplitDefaults.item} />
  </Story>
</Canvas>
