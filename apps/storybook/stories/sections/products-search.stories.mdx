import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ProductsSearch, ProductsSearchCard, ProductsSearchFilters, ProductsSearchFiltersModal } from '@websolute/ui';
import { ProductsSearchDefaults, ProductsSearchCardDefaults } from '@websolute/mock';

<Meta
  title="Sections / ProductsSearch"
  component={ProductsSearch}
  subcomponents={{ ProductsSearchCard, ProductsSearchFilters, ProductsSearchFiltersModal }}
/>

# ProductsSearch

A component to display a `ProductsSearch`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/sections/products-search)

## Imports

```js
import { ProductsSearch } from '@websolute/ui';
```

## Example

A basic example of `ProductsSearch`.

<Canvas>
  <Story name="ProductsSearch">
    <ProductsSearch items={ProductsSearchDefaults.items} />
  </Story>
</Canvas>

<Canvas>
  <Story name="ProductsSearchCard">
    <ProductsSearchCard item={ProductsSearchCardDefaults.item} maxWidth="540px" margin="3rem" />
  </Story>
</Canvas>

<Canvas>
  <Story name="ProductsSearchFilters">
    <ProductsSearchFilters maxWidth="540px" margin="3rem" />
  </Story>
</Canvas>

<Canvas>
  <Story name="ProductsSearchFiltersModal">
    <ProductsSearchFiltersModal visible={true} onClose={() => alert} />
  </Story>
</Canvas>
