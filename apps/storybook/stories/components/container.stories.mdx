import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Container, ContainerProps, Section } from '@websolute/ui';

<Meta
  title="Components / Container"
  component={Container}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Container

A component to display a `Container`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/container)

## Imports

```js
import { Container } from '@design-system';
```

## Example

A basic example of `Container`.

<Canvas>
  <Story name="Container">
    <Section background="var(--color-warning-100)">
      <Container background="var(--color-warning-200)" height="4rem"></Container>
    </Section>
  </Story>
  <Story name="Container Fluid">
    <Section background="var(--color-warning-100)">
      <Container.Fluid background="var(--color-warning-200)" height="4rem"></Container.Fluid>
    </Section>
  </Story>
</Canvas>
