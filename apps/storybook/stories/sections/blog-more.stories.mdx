import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { BlogCard, BlogMore } from '@websolute/ui';
import { BlogMoreDefaults, BlogCardDefaults } from '@websolute/mock';

<Meta
  title="Sections / BlogMore"
  component={BlogMore}
  subcomponents={{ BlogCard }}
/>

# BlogMore

A component to display a `BlogMore`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/sections/blog-more)

## Imports

```js
import { BlogMore } from '@websolute/ui';
```

## Example

A basic example of `BlogMore`.

<Canvas>
  <Story name="BlogMore">
    <BlogMore items={BlogMoreDefaults.items} />
  </Story>
</Canvas>

<Canvas>
  <Story name="BlogCard">
    <BlogCard item={BlogCardDefaults.item} maxWidth="540px" margin="3rem" />
  </Story>
</Canvas>
