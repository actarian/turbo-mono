import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Checkbox, CheckboxProps, Field, Label } from '@websolute/ui';
import Link from 'next/link';

<Meta
  title="Forms / Checkbox"
  component={Checkbox}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%', display: 'flex', justifyContent: 'center' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Checkbox

A component to display a `Checkbox`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/forms/checkbox)

## Imports

```js
import { Checkbox } from '@websolute/ui';
```

## Example

A basic example of `Checkbox`.

<Canvas>
  <Story name="default">
    <div style={{ padding: '4rem', fontSize: '2rem' }}>
      <Field>
        <Label><Checkbox name='privacy' id='privacy' /> Privacy</Label>
      </Field>
    </div>
  </Story>
</Canvas>
