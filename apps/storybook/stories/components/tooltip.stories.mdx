import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Tooltip, TooltipProps } from '@websolute/ui';
import Link from 'next/link';

<Meta
  title="Components / Tooltip"
  component={Tooltip}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%', display: 'flex', justifyContent: 'center' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Tooltip

A component to display a `Tooltip`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/tooltip)

## Imports

```js
import { Tooltip } from '@websolute/ui';
```

## Example

A basic example of `Tooltip`.

<Canvas>
  <Story name="default">
    <div style={{ padding: '4rem', fontSize: '2rem' }}>
      <span>Functional, contemporary products with an atypical and </span>
      <Tooltip text={<>Perfect for working with a CMS.</>}><u>unmistakable</u></Tooltip>
      <span>style, able to combine the highest cabinet-making tradition with the use of </span>
      <Tooltip text={<>Perfect for working with a CMS.</>} placement="bottom"><u>sophisticated</u></Tooltip>
      <span>production technologies.</span>
    </div>
  </Story>
</Canvas>
