import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Media, MediaProps } from '@websolute/ui';

<Meta
  title="Components / Media"
  component={Media}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Media

A component to display a `Media`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/media)

## Imports

```js
import { Media } from '@websolute/ui';
```

## Example

A basic example of `Media`.

<Canvas>
  <Story name="Media">
    <Media width="320px">
      <img src={'https://picsum.photos/640/480'} />
    </Media>
  </Story>
</Canvas>
