import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Section, SectionProps } from '@websolute/ui';

<Meta
  title="Components / Section"
  component={Section}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Section

A component to display a `Section`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/section)

## Imports

```js
import { Section } from '@websolute/ui';
```

## Example

A basic example of `Section`.

<Canvas>
  <Story name="Section">
    <Section></Section>
  </Story>
</Canvas>
