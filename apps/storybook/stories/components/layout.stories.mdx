import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Layout, LayoutProps } from '@websolute/ui';

<Meta
  title="Components / Layout"
  component={Layout}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Layout

A component to display a `Layout`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/layout)

## Imports

```js
import { Layout } from '@design-system';
```

## Example

A basic example of `Layout`.

<Canvas>
  <Story name="Layout">
    <Layout>Layout</Layout>
  </Story>
</Canvas>
