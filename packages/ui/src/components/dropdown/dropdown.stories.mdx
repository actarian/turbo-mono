import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Dropdown, DropdownProps } from './dropdown';
import { ChevronDown } from '@ui-icons';
import { Button, Nav, Text } from '@ui-components';
import { Field, CustomSelect, Label } from '@ui-forms';
import Link from 'next/link';

<Meta
  title="Components / Dropdown"
  component={Dropdown}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Dropdown

A component to display a `Dropdown`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/dropdown)

## Imports

```js
import { Dropdown } from '@design-system';
```

<!--
## Props

<ArgsTable of={DropdownProps} />
-->

## Example

A basic example of `Dropdown`.

<Canvas>
  <Story name="default">
    <Field>
      <Label>The Custom Select use a dropdown</Label>
      <CustomSelect name='country' id='country' defaultValue='Italia' placeholder="select a value..">
        <CustomSelect.Label>Country</CustomSelect.Label>
        <CustomSelect.Option value="it">Italia</CustomSelect.Option>
        <CustomSelect.Option value="ca">Canada</CustomSelect.Option>
        <CustomSelect.Option value="fr">Francia</CustomSelect.Option>
        <CustomSelect.Option value="de">Germania</CustomSelect.Option>
        <CustomSelect.Option value="us">Stati Uniti</CustomSelect.Option>
      </CustomSelect>
    </Field>
  </Story>
</Canvas>
