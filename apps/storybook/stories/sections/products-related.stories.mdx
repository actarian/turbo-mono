import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ProductsRelated, ProductsRelatedCard } from '@websolute/ui';
import { ProductsRelatedDefaults, ProductsRelatedCardDefaults } from '@websolute/mock';

<Meta
  title="Sections / ProductsRelated"
  component={ProductsRelated}
  subcomponents={{ ProductsRelatedCard }}
/>

# ProductsRelated

A component to display a `ProductsRelated`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/sections/products-related)

## Imports

```js
import { ProductsRelated } from '@websolute/ui';
```

## Example

A basic example of `ProductsRelated`.

<Canvas>
  <Story name="ProductsRelated">
    <ProductsRelated items={ProductsRelatedDefaults.items} />
  </Story>
  <Story name="ProductsRelatedCard">
    <ProductsRelatedCard item={ProductsRelatedCardDefaults.item} maxWidth="540px" margin="3rem" />
  </Story>
</Canvas>
