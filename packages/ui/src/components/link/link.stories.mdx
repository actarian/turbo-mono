import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { default as Link, LinkProps } from './link';

<Meta
  title="Components / Link"
  component={Link}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Link

A component to display a `Link`.

[View source](https://github.com/actarian/design-system/tree/main/src/ui/components/link)

## Imports

```js
import { Link } from '@design-system';
```

<!--
## Props

<ArgsTable of={LinkProps} />
-->

## Example

A basic example of `Link`.

<Canvas>
  <Story name="default">
    <Link href="#menu">Menu</Link>
  </Story>
</Canvas>
