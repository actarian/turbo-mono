import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Nav, NavProps, Text, Button } from '@websolute/ui';
import Link from 'next/link';

<Meta
  title="Components / Nav"
  component={Nav}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Nav

A component to display a `Nav`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/nav)

## Imports

```js
import { Nav } from '@websolute/ui';
```

## Example

A basic example of `Nav`.

<Canvas>
  <Story name="Nav.Row">
    <Nav.Row gap="3rem">
      <Link href="#marketing" passHref><Button as="a" variant="nav">Solutions</Button></Link>
      <Link href="#analytics" passHref><Button as="a" variant="nav">Company</Button></Link>
      <Link href="#commerce" passHref><Button as="a" variant="nav">Magazine</Button></Link>
      <Link href="#insights" passHref><Button as="a" variant="nav">More</Button></Link>
    </Nav.Row>
  </Story>
</Canvas>

<Canvas>
  <Story name="Nav.Col">
    <Nav.Col marginBottom="2rem" style={{ fontSize: '0.9rem' }}>
      <Text size="8">Company</Text>
      <Link href="#about" passHref><Button as="a" variant="nav">About</Button></Link>
      <Link href="#blog" passHref><Button as="a" variant="nav">Blog</Button></Link>
      <Link href="#jobs" passHref><Button as="a" variant="nav">Jobs</Button></Link>
      <Link href="#press" passHref><Button as="a" variant="nav">Press</Button></Link>
      <Link href="#partners" passHref><Button as="a" variant="nav">Partners</Button></Link>
    </Nav.Col>
  </Story>
</Canvas>
