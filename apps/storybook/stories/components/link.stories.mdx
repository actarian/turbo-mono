import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Link, LinkProps } from '@websolute/ui';

<Meta
  title="Components / Link"
  component={Link}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Link

A component to display a `Link`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/link)

## Imports

```js
import { Link } from '@websolute/ui';
```

## Example

A basic example of `Link`.

<Canvas>
  <Story name="default">
    <Link href="#menu">Menu</Link>
  </Story>
</Canvas>
