import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Page } from '@websolute/ui';
import { PageProps } from '@websolute/models';

<Meta
  title="Components / Page"
  component={Page}
  decorators={[(story) => (
    <div style={ { padding: '2rem', width: '100vw', maxWidth: '100%' } }>
      { story() }
    </div>
  )]}
  parameters={{ backgrounds: { disable: true, grid: { disable: true } } }}
/>

# Page

A component to display a `Page`.

[View source](https://github.com/actarian/turbo-mono/tree/main/packages/ui/src/components/page)

## Imports

```js
import { Page } from '@websolute/ui';
```

## Example

A basic example of `Page`.

<Canvas>
  <Story name="Page">
    <Page></Page>
  </Story>
</Canvas>
